import { useLine } from '@/lib/LineContext'

const { ready, loading, profile } = useLine()

if (loading) return <div>Loading...</div>

if (!ready) return null

return <div>{profile?.displayName}</div>